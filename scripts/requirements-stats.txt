# Pinned so CI cannot silently drift to a breaking release.
# 0.3.x removed the `Garmin.garth` API this script uses, which broke the daily
# refresh on 2026-03-21 (cached-token load failed -> fresh login -> Garmin 429).
# This combo is verified working with the existing cached OAuth tokens.
# garminconnect 0.2.28 requires garth<0.6.0,>=0.5.13; 0.5.17 satisfies it.
garminconnect==0.2.28
garth==0.5.17
